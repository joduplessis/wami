import { WEBRTC_CONFIG, WEBRTC_TOPIC } from "./";
import { Platform } from "react-native";
import {
  RTCPeerConnection,
  RTCIceCandidate,
  RTCSessionDescription,
  RTCView,
  MediaStream,
  MediaStreamTrack,
  getUserMedia
} from "react-native-webrtc";

export const webrtc = {};
